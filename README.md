```
python -m venv venv

source venv/bin/activate

pip install requests pandas python-dotenv BeautifulSoup4

python starNew.py

python info.py

GITHUB_TOKEN=你的GitHub Token
GITHUB_OWNER=仓库拥有者
GITHUB_REPO=仓库名称
GITHUB_TOKENS=你的GitHub Token池, 以逗号分隔
```